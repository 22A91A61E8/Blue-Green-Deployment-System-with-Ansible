- name: Install nginx
  apt:
    name: nginx
    update_cache: yes

- name: Copy nginx config
  template:
    src: nginx.conf.j2
    dest: /etc/nginx/sites-enabled/default

- name: Reload nginx
  service:
    name: nginx
    state: reloaded
