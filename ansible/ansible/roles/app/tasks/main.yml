- name: Install python
  raw: apt update && apt install -y python3 python3-pip

- name: Install Flask
  pip:
    name: flask

- name: Copy app
  copy:
    src: ../../../app/
    dest: /app

- name: Run app
  shell: |
    cd /app
    nohup python3 app.py > app.log 2>&1 &
